export const UsernameRegex = /^[A-Za-z\s]{1,50}$/;  // Regular expression for name
export const EmailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;  // Regular expression for email
export const passwordRegex = /^.{6,}$/;  // Regular expression for password (minimum 6 characters)
export const phoneRegex = /^\d{10}$/;  // Regular expression for phone (exactly 10 digits)
